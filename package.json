{
  "devDependencies": {
    "@swc/core": "^1.13.3",
    "elm-optimize-level-2": "^0.3.5",
    "elm-watch": "^2.0.0-beta.6"
  },
  "scripts": {
    "build": "elm-watch make --optimize && rm -r dist && mkdir dist && mkdir dist/build && cp index.html dist/index.html && cp build/main.js dist/build/main.js && cp -r public dist/public"
  }
}
